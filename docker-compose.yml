services:
  dashlet:
    build: .
    image: jaypel/dashlet:latest
    container_name: dashlet
    ports:
      - "8989:8989"
    volumes:
      # Mount the 'public' directory. Docker will populate it if empty.
      - ./app/public:/app/public
      - ./app/data:/app/data
    restart: unless-stopped
